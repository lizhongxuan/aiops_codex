# Phase B Gap List (2026-03-27)

本文档对应 `todo_remove_0325.md` 的当前实现状态，用于满足 `15.4` 的阶段性 gap 输出要求，并判断是否具备继续进入 Phase C 的条件。

## 当前结论

- Phase A 已达到“可用”目标：主机绑定、远程只读、审批续跑、停止任务、终端一致性都已闭环。
- Phase B 已完成一大半，但**还不具备整体进入 Phase C 的条件**。
- 阻塞点不在远程执行链路，而在“文件体验”和“审批工作台”两个交互面。

## 已收口能力

- `1.x` 到 `4.x` 已完成：远程 host 强绑定、只读/变更执行、审批续跑、终端一致性与资源回收已具备。
- `7.x` 已完成：命令卡成功折叠、失败展开、长脚本折叠、错误卡可重试/换方案提示已补齐。
- `9.x` 已完成：stop / timeout / reconnect / offline 的失败收口和回归测试已补齐。
- `14.3`、`14.4`、`15.1`、`15.2` 已完成：基础接入文档、smoke、单测映射已落地。

## 主要缺口

- `5.1` 到 `5.4` 未完成：过程态协议仍不够统一，顶部高亮态和底部过程行的去重/成对事件还不稳定。
- `6.1` 到 `6.4` 未完成：审批工作台虽然已具备 overlay 雏形，但队列语义、拒绝后的继续对话体验还不完整。
- `8.1` 到 `8.4` 是当前最大缺口：远程文件浏览、搜索、预览还没有完全达到“接近本地 Codex”的结构化体验。
- `10.x` 到 `12.x` 仍未整体闭环：远程 file_change 虽已有骨架，但 diff 回看、真正落盘边界、失败细节和多文件连续修改体验还不完整。
- `13.x` 未完成：安全与审计字段还有进一步固化空间。
- `14.2` 需要持续执行镜像自检：本次已补镜像最低要求校验脚本，但还没有在 CI 中固化。
- `15.3` 未完成：针对远程文件浏览/修改的端到端自动化仍缺。

## 进入 Phase C 的前置条件

建议至少满足下面 4 条后再宣布进入 Phase C：

1. `8.1` 到 `8.4` 完成，远程文件浏览/搜索结果稳定结构化。
2. `10.1` 到 `10.4` 完成，`file_change` 参数协议、失败细节、测试边界固化。
3. `11.1` 到 `11.3` 至少完成，审批卡、真正写入边界、执行结果同步到聊天流与审计日志。
4. `15.3` 至少补一条覆盖“读文件 -> 生成修改 -> 审批 -> 落盘 -> 结果回看”的端到端测试。

## 下一批建议

- 优先级 1：`8.1 / 8.2 / 8.4`
- 优先级 2：`10.1 / 10.3 / 10.4`
- 优先级 3：`11.1 / 11.2 / 11.3`
- 优先级 4：`15.3`
