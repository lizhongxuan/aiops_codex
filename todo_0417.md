# Tool Lifecycle Unification Todo

基于 [design_0417.md](/Users/lizhongxuan/Desktop/aiops-codex/design_0417.md) 的实施清单。

目标口径：

- 执行层统一成 `handler + lifecycle event`
- 展示层继续保留 `snapshot + cards + approvals + orchestrator projection`
- 让 `bifrost_runtime.go` 与 `dynamic_tools.go` 逐步并轨

---

## Phase 0: 基线确认

### [x] T0-001 梳理现有工具生命周期入口

- 文件：
  - [internal/server/bifrost_runtime.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/bifrost_runtime.go)
  - [internal/server/dynamic_tools.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/dynamic_tools.go)
  - [internal/server/tool_dispatcher.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_dispatcher.go)
- 任务：
  - 列出当前所有工具执行入口
  - 标出哪些路径直接改 `runtime/cards/approvals/evidence`
  - 标出 bifrost observer 和 dynamic tool 路径的重叠能力
- 交付：
  - 一份内部开发备注，作为后续迁移核对清单
- 验收标准：
  - 至少覆盖 `readonly_host_inspect / execute_command / read_file / list_files / search_files / write_file / web_search`
- 进展：
  - 已新增 [docs/tool-lifecycle-baseline-0417.md](/Users/lizhongxuan/Desktop/aiops-codex/docs/tool-lifecycle-baseline-0417.md)，按当前代码状态整理了 dispatcher / dynamic / bifrost 三条执行入口
  - 文档明确标出了仍直接修改 `runtime/cards/approvals/evidence` 的 legacy 直写点，以及 bifrost 与 dynamic 的重叠能力矩阵
  - 已覆盖 `readonly_host_inspect / execute_command / read_file / list_files / search_files / write_file / web_search`

### [x] T0-002 建立回归基线

- 文件：
  - [internal/server/react_loop_test.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/react_loop_test.go)
  - [internal/server/server_test.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/server_test.go)
  - [internal/server/orchestrator_integration_test.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/orchestrator_integration_test.go)
- 任务：
  - 记录当前单主机、审批、workspace、worker 的关键通过用例
  - 确认哪些测试可作为迁移后回归准线
- 交付：
  - 测试清单
- 验收标准：
  - 明确至少 8 个核心回归场景
- 进展：
  - [docs/tool-lifecycle-baseline-0417.md](/Users/lizhongxuan/Desktop/aiops-codex/docs/tool-lifecycle-baseline-0417.md) 已补齐后端 + 前端回归基线，共列出 12 个核心场景
  - 当日已验证：
    - `go test ./internal/store ./internal/server`
    - `cd web && npx vitest run tests/ChatPage.spec.js`
    - `cd web && npx vitest run tests/ProtocolWorkspacePage.spec.js --testNamePattern='renders the active mission as a process fold inside the main agent thread|opens approval evidence and submits decisions from the right rail|keeps completed turns collapsed by default while leaving the final answer visible'`
    - `cd web && PLAYWRIGHT_BASE_URL=http://127.0.0.1:4173 npx playwright test tests/e2e/chat-codex-parity.spec.js --grep '单机会话刚提问时更接近 Codex'`

---

## Phase 1: 建立统一事件模型

### [x] T1-001 新增工具生命周期模型

- 新增文件：
  - [internal/server/tool_lifecycle.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_lifecycle.go)
- 任务：
  - 定义 `ToolInvocation`
  - 定义 `ToolExecutionResult`
  - 定义 `ToolLifecycleEvent`
  - 定义 `ToolLifecycleEventType`
  - 定义 `ToolInvocationSource`
  - 定义 `ToolRunStatus`
- 验收标准：
  - 类型定义可覆盖 bifrost 和 dynamic tool 两条路径
  - 不依赖前端 card 类型

### [x] T1-002 新增统一 handler 接口与 descriptor

- 新增文件：
  - [internal/server/tool_handler.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_handler.go)
- 任务：
  - 定义 `ToolHandler`
  - 定义 `ToolDescriptor`
  - 定义 descriptor 中的只读/审批/流式能力字段
- 验收标准：
  - 允许后续同时承载 remote tool、local tool、MCP tool、orchestrator tool

### [x] T1-003 新增 registry

- 新增文件：
  - [internal/server/tool_handler_registry.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_handler_registry.go)
- 任务：
  - 注册 handler
  - 支持按 tool name 获取 descriptor 和 handler
  - 统一重复注册和覆盖策略
- 验收标准：
  - 可被 dispatcher 注入使用

---

## Phase 2: 建立事件总线与订阅器

### [x] T2-001 新增 in-process 事件总线

- 新增文件：
  - [internal/server/tool_event_bus.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_event_bus.go)
- 任务：
  - 定义 `ToolEventEmitter`
  - 定义 `ToolEventSubscriber`
  - 提供同步进程内 `Publish/Subscribe`
- 验收标准：
  - 支持多 subscriber
  - 单个 subscriber 失败不应导致总线整体崩溃

### [x] T2-002 新增投影总入口

- 新增文件：
  - [internal/server/tool_projection.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_projection.go)
- 任务：
  - 定义 `ProductProjectionSubscriber`
  - 将生命周期事件分发给多个具体 projection
- 验收标准：
  - 后续可以继续拆分具体 subscriber，不把逻辑都堆到一个函数里

### [x] T2-003 拆分 runtime projection

- 新增文件：
  - [internal/server/tool_projection_runtime.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_projection_runtime.go)
- 任务：
  - 将事件映射成 `runtime.turn.phase`
  - 将事件映射成 `runtime.activity`
- 验收标准：
  - 至少覆盖 started / approval_requested / approval_resolved / completed / failed

### [x] T2-004 拆分 card projection

- 新增文件：
  - [internal/server/tool_projection_cards.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_projection_cards.go)
- 任务：
  - started 时生成/更新 process 卡
  - completed/failed 时收敛状态
  - 保持 single_host 和 workspace 的差异策略
- 验收标准：
  - 不要求前端改代码即可继续展示

### [x] T2-005 拆分 approval projection

- 新增文件：
  - [internal/server/tool_projection_approvals.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_projection_approvals.go)
- 任务：
  - `tool.approval_requested` -> AddApproval + approval card
  - `tool.approval_resolved` -> resolve approval + memo card
- 验收标准：
  - 与当前 approval rail 行为一致

### [x] T2-006 拆分 evidence / incident projection

- 新增文件：
  - [internal/server/tool_projection_evidence.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_projection_evidence.go)
  - [internal/server/tool_projection_incidents.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_projection_incidents.go)
- 任务：
  - completed 时落 evidence
  - failed/approval 事件映射 incident event
- 进展：
  - 旧的 artifact projection 已按职责改名收口到 `tool_projection_evidence.go`；`ProductProjectionSubscriber` 默认链现在显式挂载 `evidence + incident` 两个 subscriber，而不是把证据逻辑混在 artifact 命名下
  - evidence projection 继续承接 `syncActionArtifacts` 路径，`FileChangeCard -> evidence + verification` 的既有行为保持不变，原有 evidence API 回归已通过
  - 新增 `tool_projection_incidents.go`，会把 `ToolLifecycleEventFailed` 投影为 `tool.failed`，并把 `approval_requested / approval_resolved` 直接映射到 incident timeline；approval 事件复用现有 `recordApprovalIncidentEvent()`，因此和 legacy audit 路径可以安全并存
  - 已新增 `tool_projection_incidents_test.go`，覆盖 generic failed tool incident 与 approval lifecycle incident；`tool_projection_test.go` 也已更新默认 projection 数量断言
- 验收标准：
  - evidence API 与 timeline API 仍能读到结果

### [x] T2-007 新增 snapshot broadcast subscriber

- 新增文件：
  - [internal/server/tool_projection_snapshot.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_projection_snapshot.go)
- 任务：
  - 所有事件最终走统一 snapshot broadcast
  - 内部保留节流策略
- 进展：
  - 已新增 `tool_projection_snapshot.go`，作为独立的 `SnapshotBroadcastSubscriber` 挂到 `toolEventBus` 的最后一位，顺序位于 `ProductProjectionSubscriber` 之后，确保 snapshot 刷新发生在 runtime/card/approval/evidence/incident/orchestrator 投影落库之后
  - `started / progress` 事件统一走节流广播；`completed / failed / cancelled / approval_* / choice_*` 事件会先清空 pending throttle 再立即广播，避免工具终态、审批和 choice 状态落地后还要等待下一轮节流
  - `tool_projection_cards.go` 中原来 progress 分支的直接 `throttledBroadcast()` 已移除，避免和统一 subscriber 重复发主 session snapshot；workspace 镜像相关的显式广播保持不变
  - 已新增 `tool_projection_snapshot_test.go`，覆盖节流分类、立即广播分类，以及 bus 订阅顺序必须在产品 projection 之后执行
- 验收标准：
  - 不出现 broadcast 风暴

---

## Phase 3: 建立统一 dispatcher

### [x] T3-001 升级现有 dispatcher

- 重点文件：
  - [internal/server/tool_dispatcher.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_dispatcher.go)
- 任务：
  - 将其升级为真正的 `UnifiedToolDispatcher`
  - 统一：
    - handler 查找
    - started 事件
    - 审批协调
    - execute
    - completed/failed/cancelled 事件
- 验收标准：
  - dispatcher 不直接操作 UI card
  - 状态副作用由 projection 完成

### [x] T3-002 接入 descriptor 驱动审批判断

- 文件：
  - [internal/server/tool_dispatcher.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_dispatcher.go)
  - [internal/server/tool_handler_registry.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_handler_registry.go)
- 任务：
  - 使用 `ToolDescriptor.RequiresApproval`
  - 使用 `ToolDescriptor.IsReadOnly`
- 进展：
  - `Dispatch()` 仍保留现有的 descriptor -> invocation 默认回填；本轮补齐了 batch 调度侧的缺口
  - `ToolHandlerRegistry` 已新增 `DispatchCategory()`，会在显式 `blocking/approval` 分类之外，优先使用注册 descriptor 的 `RequiresApproval / IsReadOnly` 解析最终调度类别
  - `dispatchBatch()` 已改为统一通过 registry 解析 category，不再只依赖 `lookupToolRiskMetadata()`；这让 registry 中新注册、但尚未进入静态风险表的工具，也能正确被识别为 approval/readonly
  - `tool_handler_registry_test.go` 与 `tool_dispatcher_test.go` 已新增覆盖，固定 descriptor 驱动的 approval/readonly 分类和 batch blocking 行为
- 验收标准：
  - 审批策略不再分散在多个入口判断中

### [x] T3-003 支持 progress 事件

- 文件：
  - [internal/server/tool_dispatcher.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_dispatcher.go)
  - handler 相关文件
- 任务：
  - 定义 progress callback 或 context emitter
  - 支持长命令或长任务发射进度
- 进展：
  - 已新增 `ToolLifecycleEventProgress`，并通过 `tool_progress.go` 提供基于 `context.Context` 的可选 progress reporter；不改 `ToolHandler.Execute(ctx, inv)` 接口，现有 handler 无需改签名
  - dispatcher 现在只会在 `ToolDescriptor.SupportsStreamingProgress=true` 时向 handler 注入 reporter；handler 可通过 `ReportToolProgress()` 发射 progress lifecycle event
  - card projection 已补齐 `progress` 分支：已有 `ProcessLineCard` 会更新文本并保持 `inProgress`，缺卡时则回退成一次 started 风格的过程线
  - `tool_event_store_subscriber_test.go`、`tool_dispatcher_test.go`、`tool_projection_cards_test.go` 已新增覆盖，固定 progress 的事件顺序、descriptor opt-in 行为、过程线更新和 event store 持久化
- 验收标准：
  - 不影响不支持 progress 的现有 handler

---

## Phase 4: 统一审批协调器

### [x] T4-001 新增审批协调器接口

- 新增文件：
  - [internal/server/tool_approval_coordinator.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_approval_coordinator.go)
- 任务：
  - 定义 `ToolApprovalCoordinator`
  - 统一自动放行和人工审批的返回结构
- 验收标准：
  - bifrost 与 dynamic tool 都可调用

### [x] T4-002 收口 auto-approve 逻辑

- 重点文件：
  - [internal/server/dynamic_tools.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/dynamic_tools.go)
  - [internal/server/bifrost_runtime.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/bifrost_runtime.go)
  - [internal/server/tool_approval_coordinator.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_approval_coordinator.go)
- 任务：
  - 收口：
    - session grant
    - host grant
    - policy auto-approve
    - bifrost approval resolve
- 进展：
  - 已在 `ToolApprovalCoordinator` 注册统一 `session_grant / host_grant / profile_policy` 规则
  - bifrost `requestBifrostToolApproval` 已改为通过 coordinator 统一判定自动放行，再保留 bifrost 自己的 resolve side effect
  - local command approval 与 dynamic remote operation 的自动放行匹配逻辑已改为复用同一套 coordinator 规则
  - local command / dynamic remote command / dynamic remote file change 的自动放行 notice 已统一走 approval lifecycle event + projection
- 验收标准：
  - 自动放行逻辑只保留一套

### [x] T4-003 审批事件统一投影

- 文件：
  - [internal/server/tool_projection_approvals.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_projection_approvals.go)
- 任务：
  - 人工审批和自动审批都走同一事件流
- 进展：
  - bifrost `approval_requested / approval_resolved` 已接入统一 event projection，待审批卡、自动放行 notice、人工审批 memo 现在都可通过 projection 落库
  - projection 已补齐 `detail / changes / host` 字段透传，并修复 `accepted_for_host_auto / accepted_by_policy_auto / accepted_for_session` 到 `completed` 的卡片状态映射
  - local command、dynamic remote command、dynamic remote file change 的 `approval_requested / approval_resolved` 已统一接到同一事件流；待审批卡、自动放行 notice、remote decline memo、remote accept execution card 都能通过 projection 落库
  - dynamic `request_approval` 与 `exit_plan_mode` 现在也会先发 `approval_requested` lifecycle event，再由 approval projection 落待审批卡；event emit 失败时才回退到旧的直写分支
  - `handleApprovalDecision()` 中原来直接更新 legacy approval/card 的两条通用分支，现已补上 `approval_resolved` event；非命令类审批会复用新的 resolution projection card helper，同步保持 `plan_exit` 的 phase / plan mode 语义
  - `dynamic_approval_events_test.go` 已新增覆盖，固定 `request_approval` 与 `exit_plan_mode` 两条 legacy 路径的请求/决策都会写入 `toolEventStore`
- 验收标准：
  - approval rail 不再依赖 bifrost/dynamic 两种不同逻辑

---

## Phase 5: bifrost 路径先接入统一生命周期

### [x] T5-001 改造 `OnToolStart`

- 文件：
  - [internal/server/bifrost_runtime.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/bifrost_runtime.go)
- 任务：
  - `OnToolStart` 不再直接改 runtime/card
  - 只构建 invocation/event 并 emit
- 进展：
  - 已先迁移 workspace/worker 会话下 `web_search / open_page / find_in_page / list_files / list_dir / read_file / search_files / execute_command / readonly_host_inspect / write_file / apply_patch` 的 started 事件路径
  - 本轮补齐了 `execute_readonly_query / query_ai_server_state` 的 bifrost started 事件路径；它们现在也会进入统一 lifecycle event，但通过 `skipCardProjection + trackActivityStart=false` 保持现有 `CommandCard / WorkspaceResultCard` 语义，不再额外生成 `ProcessLineCard`
  - `toolPhaseAndLabel()` 已新增 `query_ai_server_state` 标签映射，避免 started 事件落成泛化的 “执行工具”
  - 本轮进一步收掉了 `bifrostListRemoteFiles / bifrostReadRemoteFile / bifrostSearchRemoteFiles` 三条 handler 自带的 started 侧直写；它们现在复用现有 dynamic readonly handler 的纯执行结果，started 卡片与 activity 统一只由 `OnToolStart` 的 lifecycle event 落地
  - `OnToolStart` 现在在 `single_host / workspace / worker` 三类 allowlist bifrost 工具上都会优先走统一 lifecycle event；single_host 通过 `skipCardProjection` 保持“无过程线卡”的原 UI 语义
  - 当 event bus 某个 subscriber 返回错误时，`OnToolStart` 不再回退到 legacy runtime/card 直写；已新增回归固定“subscriber 失败也不会生成重复 ProcessLineCard”
- 验收标准：
  - 现有 bifrost tool 测试仍通过

### [x] T5-002 改造 `OnToolComplete`

- 文件：
  - [internal/server/bifrost_runtime.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/bifrost_runtime.go)
- 任务：
  - 将 completed / failed 的 store 副作用迁出
  - 通过 projection 落地
- 进展：
  - 已先迁移 workspace/worker 会话下 `web_search / open_page / find_in_page / list_files / list_dir / read_file / search_files / execute_command / readonly_host_inspect / write_file / apply_patch` 的 completed/failed 事件路径
  - 本轮补齐了 `execute_readonly_query / query_ai_server_state` 的 bifrost completed/failed 事件路径；completed 事件同样走统一 snapshot/projection，但跳过 process card 投影与 activity completion 计数，保持现有 `CommandCard / WorkspaceResultCard` 不倒退
  - `bifrostQueryAIServerState()` 中原有的直接 `broadcastSnapshot()` 已移除，query 工具终态刷新统一交给 lifecycle snapshot subscriber，避免 query 工具在 workspace 会话里双发 snapshot
  - `bifrostListRemoteFiles / bifrostReadRemoteFile / bifrostSearchRemoteFiles` 现在会把 `LifecycleMessage / OutputData` 暂存给 `OnToolComplete`，由 unified completion event 统一驱动 process card 与 runtime completion；remote search 的 `matchCount`、remote read 的 `ViewedFiles` 等 richer projection 已通过 `toolNameOverride` 保持不回退
  - 已新增回归测试，固定这三条 bifrost readonly remote handler 做完整 `OnToolStart -> handler -> OnToolComplete` 后只会留下 1 张 `ProcessLineCard`，且 runtime 计数不会重复累计
  - `OnToolComplete` 现在也覆盖 `single_host` allowlist bifrost 工具的 unified completion；single_host activity 继续正常清理和累计，但不会生成过程线卡
  - 当 event bus subscriber 返回错误时，`OnToolComplete` 不再回退到 legacy 完成态直写，因此不会再出现重复 completion 卡片或重复 activity 计数
- 验收标准：
  - single_host 和 workspace 的 UI 不倒退

### [x] T5-003 审核 `OnAssistantDelta / OnStreamComplete` 边界

- 文件：
  - [internal/server/bifrost_runtime.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/bifrost_runtime.go)
- 任务：
  - 确认 assistant 文本流仍由现有 stream observer 驱动
  - 工具生命周期与 assistant 流不混淆
- 进展：
  - 已新增 `bifrost_tool_lifecycle_test.go` 回归，固定 `OnAssistantDelta -> query_ai_server_state -> OnStreamComplete` 的边界：assistant 文本继续落在 `AssistantMessageCard`，工具结果落在独立 `WorkspaceResultCard`，两者不会复用同一张卡
  - 新增测试还固定了 `OnStreamComplete` 在存在 tool call 时不会把 turn phase 错误推进到 `finalizing`，assistant final card 逻辑保持原样
- 验收标准：
  - assistant final card 逻辑保持原样

---

## Phase 6: dynamic tool 路径迁移到统一 dispatcher

### [x] T6-001 迁移只读工具

- 重点文件：
  - [internal/server/dynamic_tools.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/dynamic_tools.go)
- 优先工具：
  - `readonly_host_inspect`
  - `query_ai_server_state`
  - `execute_readonly_query`
  - `list_files`
  - `read_file`
  - `search_files`
- 任务：
  - 当前函数改为构造 invocation 并调用 dispatcher
- 进展：
  - 已新增统一注册的 dynamic readonly handler，并接入 `toolDispatcher`
  - `execute_readonly_query` 已改为走 dispatcher；handler 内部仍复用现有 `runRemoteExec / runLocalReadonlyExec`，确保 `CommandCard`、evidence、audit 和 stdout/stderr 汇总逻辑不倒退
  - `readonly_host_inspect` 的无审批执行分支也已改为走 dispatcher；保留原有 host 选择、reason 校验、只读命令校验和“需要审批时仍走 approval rail”的行为
  - 为命令类只读工具新增 `skipCardProjection` 控制，生命周期事件现在会进入 event store，但不会额外生成 `ProcessLineCard` 或覆盖已有 `CommandCard`
  - `react_loop_test.go` 已新增覆盖，固定 workspace `readonly_host_inspect` 的单张 `CommandCard`、snapshot tool invocation 和 started/completed lifecycle event 行为
  - `list_remote_files / read_remote_file / search_remote_files` 已改为构造 invocation 后走 dispatcher；过程线卡片继续保留 `process-toolcmd-*` 形式的 ID
  - lifecycle payload 现已支持自定义 completion/failure 文案和 projection payload；runtime projection 已补齐 remote alias 的 `ViewedFiles / SearchLocationCount / SearchQuery` 副作用
  - `dynamic_tools_policy_test.go` 已新增覆盖，固定 `execute_readonly_query` 的无审批、保留单张 `CommandCard`、started/completed event 和无额外过程线卡行为
  - `remote_status_unify_test.go` 与 `tool_projection_runtime_test.go` 已新增覆盖，固定 remote list/read/search 的 started/completed 事件、process card、activity 计数与无 `ResultSummaryCard` 行为
  - `query_ai_server_state` 已改为走 dispatcher；handler 内部保留 `WorkspaceResultCard + evidence` 直写语义，同时通过 `skipCardProjection` 避免额外生成过程线卡
  - `evidence_binder_test.go` 与 `dynamic_tools_policy_test.go` 已新增覆盖，固定 `query_ai_server_state` 的单张 `WorkspaceResultCard`、evidence 绑定、structured tool response 与 started/completed lifecycle event 行为
- 验收标准：
  - 结果卡、过程线、evidence 与当前行为一致

### [x] T6-002 迁移命令审批工具

- 优先工具：
  - `execute_command`
  - local mutation command
  - remote mutation command
- 任务：
  - 将审批创建和 resolve 流程迁到 coordinator + event projection
- 进展：
  - `handleLocalCommandApprovalRequest`、`requestRemoteCommandApproval`、remote command `handleApprovalDecision` 已改为统一走 `approval_requested / approval_resolved` event projection
  - session grant / host grant / profile policy 的 local/remote command auto-approve notice 已统一使用 lifecycle event helper 落库
  - 新增 `dynamic_approval_events_test.go` 覆盖 remote command pending、local command profile auto-approve 回归
  - local / remote command approval 创建路径现在统一通过 `ToolApprovalCoordinator.Request()` 产出 pending/auto-approved resolution，不再由入口函数自己分散判断 `session_grant / host_grant / profile_policy`
  - command approval 的 `approvalId / requestedAt` 已对齐 coordinator 返回值；local profile auto-approve 继续保留 `accepted_by_profile_auto` 旧状态语义，remote command 继续保留执行后再回 Codex 的行为
  - `dynamic_approval_events_test.go` 已新增覆盖，固定 remote command pending approval 和 local command auto-approve 都真正经过 coordinator `Request()`，避免后续回退到只调用 `AutoApprove()`
- 验收标准：
  - 审批卡 UI 不变
  - 自动放行逻辑不回归

### [x] T6-003 迁移文件变更工具

- 优先工具：
  - `write_file`
  - file change approval 相关逻辑
- 任务：
  - 将文件变更执行与审批生命周期统一
- 进展：
  - `requestRemoteFileChangeApproval`、remote file change auto-approve、remote file change `handleApprovalDecision` 已改为统一走 `approval_requested / approval_resolved` event projection
  - remote file change `started / completed / failed` 已统一发出 lifecycle event，`ProcessLineCard`、runtime activity、最终 `FileChangeCard` 通过 projection 落库
  - 已新增 artifact projection subscriber；带 `finalCard` 的文件变更完成态事件现在会统一绑定 `evidenceId` 并触发 `syncActionVerification`
  - `executeApprovedRemoteFileChange` 仅保留 audit、Codex response 和 event 失败时的 fallback 直写逻辑
- 验收标准：
  - 文件变更卡和 verification 不倒退

---

## Phase 7: orchestrator 与 workspace 投影收口

### [x] T7-001 新增 orchestrator subscriber

- 新增文件：
  - [internal/server/tool_projection_orchestrator.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_projection_orchestrator.go)
- 任务：
  - worker phase 同步从直接调用改为订阅事件
- 进展：
  - 已新增 `tool_projection_orchestrator.go`，订阅 `approval_requested / approval_resolved` lifecycle event
  - worker session 的 orchestrator phase 现在通过 subscriber 调 `SyncWorkerPhase`，再统一触发 `syncWorkspaceMissionRuntime + refreshWorkspaceProjection`
  - 已补测试覆盖 `waiting_approval -> executing` 的 worker/task/runtime 投影联动
- 验收标准：
  - `waiting_approval / executing / completed / failed` 的 worker phase 行为一致

### [x] T7-002 清理工具层直接操作 orchestrator 的逻辑

- 文件：
  - [internal/server/dynamic_tools.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/dynamic_tools.go)
  - [internal/server/bifrost_runtime.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/bifrost_runtime.go)
- 任务：
  - 将直接 `SyncWorkerPhase` 和镜像审批等逻辑逐步改成 subscriber 处理
- 进展：
  - dynamic / bifrost / local approval request 的 worker `waiting_approval` 直调已改成 “event 成功走 subscriber，event 失败才 fallback”
  - remote/local/bifrost approval resolved 的 worker `executing / thinking` 直调已按同样方式收口到 event subscriber
  - 已新增 `syncWorkerPhaseAndRefreshWorkspace` helper，worker `waiting_input / thinking / executing` 的 direct `SyncWorkerPhase` 已全部收口到统一入口
  - `createChoiceRequest()` / `submitChoiceAnswer()` 现在优先发 `choice_requested / choice_resolved` lifecycle event，由 subscriber 负责 worker route 注册、worker phase 同步、workspace 镜像卡片和 orchestrator choice record
  - `mirrorInternalChoiceToWorkspace()` 已收缩成纯 workspace mirror helper，只在 emit 失败 fallback 时配合 `projectChoiceRequestedFallback()` 使用
  - `mirrorInternalApprovalToWorkspace()` 已收缩成纯 workspace mirror helper；approval route 注册、worker `waiting_approval` phase、workspace 镜像和 queued worker 激活统一由 orchestrator subscriber / `projectApprovalRequestedFallback()` 承接
  - `handleLocalCommandApprovalRequest()`、dynamic approval request 三条路径、`requestBifrostToolApproval()` 和旧 `bifrostRequestApproval()` 都已改成 “emit `approval_requested` -> subscriber -> emit 失败 fallback”，不再由入口直接触碰 orchestrator
  - 已补 `tool_projection_orchestrator_test.go` 和 `bifrost_approval_test.go`，覆盖 approval route 注册、workspace mirror，以及旧 bifrost `request_approval` 入口的 lifecycle event 发射
  - choice mirror、choice answer、non-bifrost worker turn start 的 workspace runtime / mission projection 已通过该 helper 和 lifecycle subscriber 统一刷新
  - 已修复 worker budget 统计把 workspace turn 误算为 active worker 的回归，恢复 `Budgeted fanout` e2e 里 mission budget=4 时能并发启动 4 个 worker
- 验收标准：
  - 工具 handler 不直接感知 orchestrator

---

## Phase 8: event store 与 replay 基础

### [x] T8-001 新增 tool event store

- 新增文件：
  - [internal/store/tool_event_store.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/store/tool_event_store.go)
  - [internal/server/tool_event_store_subscriber.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_event_store_subscriber.go)
- 任务：
  - 先做内存 ring buffer
  - 支持按 session 查询事件
- 进展：
  - 已新增 `ToolEventStore`，使用内存 ring buffer 保存最近 tool lifecycle event，并在写入时分配单调 `Sequence`
  - `server.New()` 现在会先注册 `toolEventStoreSubscriber`，再注册产品 projection，确保 dispatcher 和手工 `Emit` 的事件都会被统一记录
  - 已补 `internal/store/tool_event_store_test.go` 和 `internal/server/tool_event_store_subscriber_test.go`，覆盖 ring wrap、session 顺序查询、payload/metadata clone 和 bus 订阅接入
- 验收标准：
  - 允许测试验证事件顺序

### [x] T8-002 接 timeline/evidence 调试接口

- 文件：
  - [internal/server/evidence_api.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/evidence_api.go)
  - [internal/server/orchestrator_history.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/orchestrator_history.go)
- 任务：
  - 根据需要开放只读调试接口，便于确认投影结果
- 进展：
  - 已新增 `GET /api/v1/sessions/{sessionID}/tool-events` 和 legacy `/api/sessions/{sessionID}/tool-events`
  - 调试接口直接读取 `toolEventStore`，支持按 `type` / `tool` 过滤和 `limit` 截断，不改变现有 incident timeline 语义
  - 已补 `evidence_api_test.go` 覆盖 owned-session route、过滤条件和 tool event payload 编码
- 验收标准：
  - 不影响现有 API 兼容性

---

## 测试清单

### 单元测试

#### TT-001 lifecycle event 顺序

- 文件：
  - [internal/server/tool_dispatcher_test.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_dispatcher_test.go)
- 场景：
  - started -> completed
  - started -> failed
  - started -> approval_requested -> approval_resolved -> completed

#### TT-002 projection 映射

- 文件：
  - [internal/server/tool_projection_test.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_projection_test.go)
- 场景：
  - started 更新 runtime/activity
  - approval_requested 生成 approval
  - completed 落卡
  - failed 落 error / incident

#### TT-003 approval coordinator

- 文件：
  - [internal/server/tool_approval_coordinator_test.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/tool_approval_coordinator_test.go)
- 场景：
  - session grant 自动通过
  - host grant 自动通过
  - policy 自动通过
  - 人工审批通过
  - 人工审批拒绝

### 集成测试

#### TT-004 bifrost tool path

- 文件：
  - [internal/server/bifrost_runtime_test.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/bifrost_runtime_test.go)
- 场景：
  - bifrost 工具调用后仍能产出相同 snapshot/card

#### TT-005 dynamic tool path

- 文件：
  - [internal/server/server_test.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/server_test.go)
- 场景：
  - readonly tool
  - command approval
  - file change approval

#### TT-006 workspace / worker

- 文件：
  - [internal/server/orchestrator_integration_test.go](/Users/lizhongxuan/Desktop/aiops-codex/internal/server/orchestrator_integration_test.go)
- 场景：
  - worker waiting_approval
  - worker executing
  - worker completed

### [x] 前端回归

#### [x] TT-007 snapshot 驱动 UI 不倒退

- 文件：
  - [web/tests](/Users/lizhongxuan/Desktop/aiops-codex/web/tests)
- 场景：
  - thinking/process line
  - approval rail
  - final assistant conclusion
- 进展：
  - `ChatPage.spec.js` 已通过，覆盖 single-host chat 的 working/process line、approval overlay 与 final answer 可见性
  - `ProtocolWorkspacePage.spec.js` 定向 3 用例已通过，覆盖 workspace 主线程 process fold、右侧 approval rail 与完成态 final answer
  - 最小 Playwright smoke `chat-codex-parity.spec.js --grep '单机会话刚提问时更接近 Codex'` 已通过，确认浏览器层 Working 状态未倒退

---

## 分阶段验收

### Milestone A

范围：

- 完成 Phase 1-3
- bifrost 路径发事件但 UI 仍旧正常

验收标准：

- dispatcher + event bus + runtime/card/approval projection 可运行
- 现有核心测试不倒退

### Milestone B

范围：

- 完成 Phase 4-6
- dynamic tool 主路径迁移

验收标准：

- 动态工具路径不再直接手写大部分副作用
- 审批统一收口

### Milestone C

范围：

- 完成 Phase 7-8

验收标准：

- orchestrator phase 同步改为事件投影
- event store 能支撑调试与后续 replay

---

## 建议实施顺序

建议按这个顺序推进：

1. Phase 1
2. Phase 2
3. Phase 3
4. Phase 5
5. Phase 4
6. Phase 6
7. Phase 7
8. Phase 8

原因：

- 先建立事件和投影骨架
- 再先迁 bifrost 路径，风险更可控
- 审批统一要放在 dispatcher 稳定后
- dynamic tool 最杂，适合后迁

---

## 首批建议直接开工的任务

建议第一批只做这 6 个：

- T1-001 新增工具生命周期模型
- T1-002 新增统一 handler 接口与 descriptor
- T2-001 新增 in-process 事件总线
- T2-003 拆分 runtime projection
- T2-004 拆分 card projection
- T3-001 升级现有 dispatcher

完成这 6 个后，再开始 bifrost 路径接入。
